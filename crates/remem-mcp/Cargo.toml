[package]
name = "remem-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
remem-types = { path = "../remem-types" }
remem-store = { path = "../remem-store" }
remem-graph = { path = "../remem-graph" }
remem-embed = { path = "../remem-embed" }
remem-recall = { path = "../remem-recall" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
